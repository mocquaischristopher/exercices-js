/*  Votre code Javascript vient ici:
      - Vous pourrez visualiser le résultat en ouvrant index.html dans votre navigateur.
      - N'oubliez pas de sauvegarder pour afficher les changements.
*/


/**
 * Réécrivez ce `if` en utilisant l'opérateur conditionnel `'?'` :
if (a + b < 4) {
  result = 'Below';
} else {
  result = 'Over';
}

 */

let result = (a + b < 4) ? 'Below':'Over';

/**
 * Mon raisonnement est qu'a la place d'un if/else, 
 * je crée une variable result ou je stocke la variable dedans et ensuite,
 * cela ressort 'Below' si c'est vrai ou 'Over' si c'est faux
 */